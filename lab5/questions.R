#war count the number of chars within a string without space; count the number of vowels

#war to extract substring of 6 characters from given string and replace that substring with "Odisha" 

#reverse an entire string and display

#search a specific substring from a string and display reverse now insert a new string at that position

#create a csv as stud.csv 5 cols (Roll, name, bracnh, %age, doa). now read the stud.csv, display that
 ''' write a subset of record whose %>80 to a new csv'''
 
 '''Retrieve and display details of that student who has max percentage'''
 #Retrieve & display those cse students whose address is bbsr & is >90%
 
 # who are admitted on or after 1st june 2016